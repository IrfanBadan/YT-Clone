function toggleSidebar() { const sidebar = document.getElementById("sidebar"); const isOpen = sidebar.style.left === "0px"; if (isOpen) { sidebar.style.left = "-280px"; document.removeEventListener("click", outsideClickListener); } else { sidebar.style.left = "0px"; setTimeout(() => { document.addEventListener("click", outsideClickListener); }, 0); ; } } const loginBtn =document.getElementById("login-btn") 